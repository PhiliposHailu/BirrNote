// This is a lightweight container to transport
// calculated trend bars (Label name and Total money spent) to our UI.
class TrendBarData {
  final String label;
  final double total;

  TrendBarData(this.label, this.total);
}